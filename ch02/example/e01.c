#include <stdio.h>
int main(int argc, char const *argv[]) {
    int x, y;
    x = 899;
    y = 234;
    printf("x + y = %d\n", x + y);
    printf("x - y = %d\n", x - y);
    printf("x * y = %d\n", x * y);
    printf("x / y = %d\n", x / y);
    printf("x %% y = %d\n", x % y);
    return 0;
}